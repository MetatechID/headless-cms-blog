import React from "react";

const SectionHeading = ({ children }) => {
  return (
    <h2 className="mb-8 text-3xl font-medium capitalize text-[#7C7D83]">
      {children}
    </h2>
  );
};

export default SectionHeading;
